const imageSlide = (Newsrc) => { 
    const mainImage = document.querySelector(".starBucks") ; 
    mainImage.src = Newsrc ;
} 
const changeCircleBackground = (color) => { 
const circle = document.querySelector(".circle") ; 
circle.style.background = color ;
}